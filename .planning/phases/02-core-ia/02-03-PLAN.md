---
phase: 02-core-ia
plan: 03
type: execute
wave: 3
depends_on: ["02-01"]
files_modified:
  - src/app/(dashboard)/chat/page.tsx
  - src/components/chat/chat-window.tsx
  - src/components/chat/message-bubble.tsx
  - src/components/chat/tool-loading.tsx
  - src/components/chat/tool-result.tsx
  - src/components/chat/input-bar.tsx
autonomous: true
requirements: [RF-02.1, RF-02.4, RF-02.5]

must_haves:
  truths:
    - "Usuario digita mensagem e ve resposta da Anja em streaming"
    - "Mensagens do usuario aparecem alinhadas a direita com accent gold"
    - "Mensagens da Anja aparecem alinhadas a esquerda com bg char2"
    - "Durante tool execution, 3-dot spinner aparece com texto descritivo (ex: Consultando agenda...)"
    - "Resultado de tool aparece inline com checkmark e resumo"
    - "Erro de tool aparece inline com icone de alerta e mensagem"
    - "Input bar fixo no bottom com textarea que cresce, botao gold, Enter para enviar"
    - "Botao de enviar fica desabilitado durante streaming"
  artifacts:
    - path: "src/components/chat/chat-window.tsx"
      provides: "Container principal do chat com useChat"
      exports: ["ChatWindow"]
    - path: "src/components/chat/message-bubble.tsx"
      provides: "Bolha de mensagem com tool invocations"
      exports: ["MessageBubble"]
    - path: "src/components/chat/tool-loading.tsx"
      provides: "Loading state durante tool execution"
      exports: ["ToolLoading"]
    - path: "src/components/chat/tool-result.tsx"
      provides: "Resultado inline de tool"
      exports: ["ToolResult", "ToolError"]
    - path: "src/components/chat/input-bar.tsx"
      provides: "Barra de input com textarea e botao"
      exports: ["InputBar"]
    - path: "src/app/(dashboard)/chat/page.tsx"
      provides: "Pagina de chat atualizada"
  key_links:
    - from: "src/components/chat/chat-window.tsx"
      to: "/api/chat"
      via: "useChat({ api: '/api/chat' })"
      pattern: "useChat.*api.*chat"
    - from: "src/components/chat/message-bubble.tsx"
      to: "src/components/chat/tool-loading.tsx"
      via: "renderiza ToolLoading quando toolInvocation.state === 'call'"
      pattern: "ToolLoading"
    - from: "src/components/chat/message-bubble.tsx"
      to: "src/components/chat/tool-result.tsx"
      via: "renderiza ToolResult quando toolInvocation.state === 'result'"
      pattern: "ToolResult"
---

<objective>
Criar a interface de chat completa com streaming, tool invocation states e input bar.

Purpose: Lado cliente do chat — o usuario interage com a Anja via interface visual que mostra respostas em streaming, loading de tools e resultados inline.
Output: 6 arquivos de componentes + pagina de chat atualizada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-core-ia/02-01-SUMMARY.md

<interfaces>
From Vercel AI SDK (ai/react):
```typescript
import { useChat } from 'ai/react'
// useChat returns:
// messages: Message[] — array de mensagens com toolInvocations
// input: string — valor atual do input
// handleInputChange: (e) => void
// handleSubmit: (e) => void
// isLoading: boolean — true durante streaming
// error: Error | undefined

// Message type:
interface Message {
  id: string
  role: 'user' | 'assistant' | 'system' | 'data'
  content: string
  toolInvocations?: ToolInvocation[]
}

interface ToolInvocation {
  toolCallId: string
  toolName: string
  args: Record<string, unknown>
  state: 'call' | 'partial-call' | 'result'
  result?: unknown
}
```

From src/components/ui/spinner.tsx:
```typescript
export function Spinner(): JSX.Element  // 3-dot bounce, bg-gold
```

From src/components/ui/button.tsx:
```typescript
export function Button(props: { variant?: 'primary' | 'secondary' | 'ghost', loading?: boolean, ... }): JSX.Element
```

Design tokens (globals.css):
```
--charcoal, --char2, --char3, --gold, --gold-light, --cream, --text-muted, --danger, --success
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar componentes de tool state (ToolLoading + ToolResult)</name>
  <files>src/components/chat/tool-loading.tsx, src/components/chat/tool-result.tsx</files>
  <action>
1. Criar `src/components/chat/tool-loading.tsx`:
   - 'use client'
   - Importar Spinner de `@/components/ui/spinner`
   - Mapa `TOOL_LABELS` com labels PT-BR:
     - `create_calendar_event` -> "Criando evento na agenda"
     - `list_calendar_events` -> "Consultando agenda"
     - `create_task` -> "Criando tarefa"
     - `list_tasks` -> "Consultando tarefas"
   - Props: `toolName: string`
   - Renderizar: container com `bg-char-2 border border-char-3 rounded-lg px-3 py-2 flex items-center gap-2`
   - Dentro: `<Spinner />` + `<span className="text-sm text-cream">{label}...</span>`
   - Fallback se toolName nao esta no mapa: "Processando"

2. Criar `src/components/chat/tool-result.tsx`:
   - 'use client'
   - Importar CheckCircle2 e AlertCircle de lucide-react
   - Interface `ToolResultData`: `{ success: boolean, data?: unknown, error?: string }`
   - Props: `toolName: string, result: unknown`
   - Cast result para ToolResultData
   - Se `!result.success`: renderizar ToolError com icone AlertCircle (text-danger) + mensagem de erro
   - Se `result.success`: renderizar com icone CheckCircle2 (text-success) + texto resumo baseado em toolName
     - `create_calendar_event`: "Evento criado" + checkmark
     - `list_calendar_events`: "Agenda consultada" + checkmark
     - `create_task`: "Tarefa criada" + checkmark
     - `list_tasks`: "Tarefas consultadas" + checkmark
     - Fallback: "Concluido" + checkmark
   - Estilo: `bg-char-2 border border-char-3 rounded-lg px-3 py-2 flex items-center gap-2 text-sm`
   - Exportar ToolError separadamente para uso direto se necessario

Font weights: apenas font-normal (400) e font-semibold (600). NAO usar font-medium.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/chat/tool-loading.tsx src/components/chat/tool-result.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Ambos os componentes compilam. ToolLoading mostra spinner + label PT-BR. ToolResult mostra checkmark/erro inline.</done>
</task>

<task type="auto">
  <name>Task 2: Criar MessageBubble, InputBar, ChatWindow e atualizar chat page</name>
  <files>src/components/chat/message-bubble.tsx, src/components/chat/input-bar.tsx, src/components/chat/chat-window.tsx, src/app/(dashboard)/chat/page.tsx</files>
  <action>
1. Criar `src/components/chat/message-bubble.tsx`:
   - 'use client'
   - Importar Message de 'ai' (type import), ToolLoading, ToolResult
   - Props: `message: Message`
   - Se `message.role === 'user'`:
     - Alinhar a direita: `flex justify-end`
     - Bolha: `bg-gold/10 border border-gold/20 text-cream rounded-2xl rounded-br-md px-4 py-2.5 max-w-[85%] md:max-w-[70%]`
   - Se `message.role === 'assistant'`:
     - Alinhar a esquerda: `flex justify-start`
     - Bolha: `bg-char-2 border border-char-3 text-cream rounded-2xl rounded-bl-md px-4 py-2.5 max-w-[85%] md:max-w-[70%]`
   - Renderizar `message.content` como texto (preservar quebras de linha com `whitespace-pre-wrap`)
   - Apos o content, iterar sobre `message.toolInvocations` (se existir):
     - Se `state === 'call' || state === 'partial-call'`: renderizar `<ToolLoading toolName={tool.toolName} />`
     - Se `state === 'result'`: renderizar `<ToolResult toolName={tool.toolName} result={tool.result} />`
   - NAO renderizar mensagens com role 'system' ou 'data'
   - Font weight: text sem font-medium, usar font-normal (padrao)

2. Criar `src/components/chat/input-bar.tsx`:
   - 'use client'
   - Importar SendHorizonal de lucide-react
   - Props: `input: string, handleInputChange: (e: React.ChangeEvent<HTMLTextAreaElement>) => void, handleSubmit: (e: React.FormEvent) => void, isLoading: boolean`
   - Container fixo no bottom: `sticky bottom-0 bg-charcoal border-t border-char-3 p-3 md:p-4`
   - Form com `onSubmit={handleSubmit}` e `flex items-end gap-2`
   - Textarea (NAO input): `bg-char-2 border border-char-3 text-cream rounded-xl px-4 py-3 flex-1 resize-none focus:outline-none focus:border-gold/40 placeholder:text-muted text-sm`
     - `rows={1}` com altura dinamica via CSS: `min-h-[44px] max-h-[120px]` e `overflow-y: auto`
     - `placeholder="Mensagem para Anja..."`
     - `value={input}` e `onChange={handleInputChange}`
     - onKeyDown: Enter sem Shift envia o form (`e.preventDefault()` + `handleSubmit(e)`). Shift+Enter faz nova linha.
   - Botao de enviar: `bg-gold text-charcoal rounded-xl p-3 hover:bg-gold-light transition-colors disabled:opacity-40 disabled:cursor-not-allowed`
     - Disabled quando `isLoading || !input.trim()`
     - Icone SendHorizonal tamanho `w-4 h-4`

3. Criar `src/components/chat/chat-window.tsx`:
   - 'use client'
   - Importar `useChat` de `ai/react`
   - Importar MessageBubble, InputBar
   - Importar `useRef, useEffect` de react
   - `useChat({ api: '/api/chat', maxSteps: 5 })` — maxSteps DEVE ser 5 para sincronizar com server
   - Layout: flex flex-col h-full (preenche o main content area)
   - Area de mensagens: `flex-1 overflow-y-auto px-3 md:px-6 py-4 space-y-3`
     - Ref para auto-scroll: `messagesEndRef` com `useEffect` que faz scroll quando messages muda
     - Mapear messages para `<MessageBubble key={m.id} message={m} />`
     - Estado vazio (sem mensagens): mostrar saudacao centralizada
       - Texto: "Ola! Sou a Anja, sua secretaria executiva." em font-display text-gold
       - Subtexto: "Como posso ajudar voce hoje?" em text-muted text-sm
   - InputBar fixo no bottom
   - Se `error`: mostrar banner de erro acima do InputBar com text-danger
   - `onError`: log no console

4. Atualizar `src/app/(dashboard)/chat/page.tsx`:
   - Substituir conteudo placeholder por `<ChatWindow />`
   - A page nao precisa de 'use client' — o ChatWindow ja e client component
   - Layout: o ChatWindow deve preencher todo o espaco disponivel. Adicionar classes no wrapper:
     `<div className="flex flex-col h-[calc(100vh-var(--header-height,64px))] -m-4 md:-m-6 -mb-20 md:-mb-6">`
     Isso remove o padding do main e faz o chat ocupar tela inteira.
     ALTERNATIVA mais simples se o calculo de altura nao funcionar: usar `fixed inset-0` com offsets para sidebar/header. Testar e ajustar.

Font weights: apenas font-normal e font-semibold. NAO usar font-medium em nenhum componente.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/chat/chat-window.tsx src/components/chat/message-bubble.tsx src/components/chat/input-bar.tsx src/app/(dashboard)/chat/page.tsx 2>&1 | head -30</automated>
  </verify>
  <done>Todos os componentes compilam. ChatWindow usa useChat apontando para /api/chat. MessageBubble renderiza user/assistant com estilos corretos. InputBar tem textarea + botao gold. ToolLoading e ToolResult aparecem inline durante/apos tool execution. Chat page atualizada com ChatWindow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> useChat | Texto do usuario enviado ao backend via fetch |
| toolInvocations render | Dados retornados por tools renderizados na UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | T (Tampering) | user input | mitigate | Textarea com onSubmit controlado — input sanitizado pelo Vercel AI SDK antes de envio. |
| T-02-08 | I (Information Disclosure) | tool result render | mitigate | ToolResult faz cast para interface conhecida e renderiza apenas campos esperados (success, error, data). Nunca renderiza objeto raw com JSON.stringify. |
</threat_model>

<verification>
- `npx tsc --noEmit` compila sem erros
- Chat page renderiza ChatWindow
- Mensagens user = direita com gold accent
- Mensagens assistant = esquerda com char2 bg
- ToolLoading mostra spinner + label PT-BR
- ToolResult mostra checkmark ou erro inline
- InputBar com textarea, Enter envia, Shift+Enter nova linha
- Auto-scroll ao receber novas mensagens
</verification>

<success_criteria>
- Interface de chat completa e funcional
- Streaming visual (texto aparece progressivamente)
- Tool states visiveis (loading -> resultado ou erro)
- Input bar responsiva com textarea expansivel
- Nenhum font-medium usado — apenas font-normal e font-semibold
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ia/02-03-SUMMARY.md`
</output>
