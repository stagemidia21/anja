---
phase: 01-fundacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.mjs
  - .env.local.example
  - .gitignore
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "npm run dev inicia sem erros na porta 3000"
    - "TypeScript compila sem erros"
    - "Dependências essenciais instaladas (supabase/ssr, lucide-react, ai)"
  artifacts:
    - path: "package.json"
      provides: "Projeto Next.js 14 com todas as dependências"
      contains: "@supabase/ssr"
    - path: "next.config.mjs"
      provides: "Configuração Next.js"
    - path: ".env.local.example"
      provides: "Template de variáveis de ambiente"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
    - path: "src/app/layout.tsx"
      provides: "Root layout"
    - path: ".gitignore"
      provides: "Ignora node_modules, .env.local, .next"
  key_links:
    - from: "package.json"
      to: "node_modules"
      via: "npm install"
      pattern: "next.*14"
---

<objective>
Criar o projeto Next.js 14 com App Router do zero usando create-next-app, instalar todas as dependências da Fase 1 e configurar o ambiente base.

Purpose: Fundação do projeto — sem isso nada mais roda.
Output: Projeto Next.js funcional com todas as deps, pronto para receber design system e auth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/stack-nextjs-supabase-claude.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar projeto Next.js 14 com create-next-app</name>
  <files>package.json, tsconfig.json, next.config.mjs, .gitignore, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/STATE.md
  </read_first>
  <action>
Executar create-next-app no diretório atual do projeto (c:/Users/homer/ccos-ratos/projetos/anja/). O projeto deve ser criado NA RAIZ deste diretório, não em um subdiretório.

```bash
cd c:/Users/homer/ccos-ratos/projetos/anja
npx create-next-app@14 . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-turbo
```

Se o diretório não estiver vazio (por causa de .planning), usar a flag que permite diretórios não-vazios ou mover .planning temporariamente.

Após criação, verificar que a estrutura é:
- src/app/layout.tsx (root layout)
- src/app/page.tsx (home page)
- src/app/globals.css (será reescrito no Plan 02)
- next.config.mjs
- tailwind.config.ts
- tsconfig.json
- package.json

Limpar o page.tsx padrão — substituir por um placeholder simples:
```tsx
export default function Home() {
  return (
    <main className="min-h-screen bg-charcoal flex items-center justify-center">
      <h1 className="text-cream text-xl">Anja</h1>
    </main>
  );
}
```

Limpar o layout.tsx padrão — remover metadata do template default e deixar como:
```tsx
import type { Metadata } from 'next';
import './globals.css';

export const metadata: Metadata = {
  title: 'Anja — Secretária Executiva com IA',
  description: 'Sua secretária executiva com IA para empreendedores brasileiros.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
```

NOTA: As fontes serão configuradas no Plan 02 (design system). NÃO configurar fontes aqui.
  </action>
  <verify>
    <automated>cd c:/Users/homer/ccos-ratos/projetos/anja && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contém `"next": "14` (grep -c '"next": "14' package.json retorna 1)
    - `src/app/layout.tsx` existe com `lang="pt-BR"`
    - `src/app/page.tsx` existe com texto "Anja"
    - `npx next build` completa sem erros
  </acceptance_criteria>
  <done>Projeto Next.js 14 criado com App Router, TypeScript, Tailwind, ESLint. Build passa.</done>
</task>

<task type="auto">
  <name>Task 2: Instalar dependências e criar .env.local.example</name>
  <files>package.json, .env.local.example</files>
  <read_first>
    .planning/STATE.md
    .planning/research/stack-nextjs-supabase-claude.md
  </read_first>
  <action>
Instalar todas as dependências necessárias para a Fase 1:

```bash
cd c:/Users/homer/ccos-ratos/projetos/anja

# Auth e banco
npm install @supabase/ssr @supabase/supabase-js

# Ícones
npm install lucide-react

# AI SDK (para fases futuras, mas instalar agora para evitar refatoração)
npm install ai @ai-sdk/anthropic
```

NÃO instalar: `@supabase/auth-helpers-nextjs` (deprecated), `jsonwebtoken` (problemas com Edge), shadcn/ui, radix-ui.

Criar `.env.local.example` com todas as variáveis que o projeto precisa (sem valores reais):

```
# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google OAuth (configurar no Google Cloud Console)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback

# Anthropic (para Fase 2+)
ANTHROPIC_API_KEY=your_anthropic_api_key
```

Verificar que `.gitignore` inclui `.env.local` (create-next-app já adiciona por padrão). Se não incluir, adicionar.

Adicionar ao `.gitignore` se não existir:
```
.env.local
.env*.local
```
  </action>
  <verify>
    <automated>cd c:/Users/homer/ccos-ratos/projetos/anja && node -e "const pkg = require('./package.json'); const deps = {...pkg.dependencies, ...pkg.devDependencies}; const required = ['@supabase/ssr', '@supabase/supabase-js', 'lucide-react', 'ai']; const missing = required.filter(d => !deps[d]); if (missing.length) { console.error('MISSING:', missing); process.exit(1); } console.log('All deps present');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contém `@supabase/ssr` (grep -c "@supabase/ssr" package.json retorna 1)
    - `package.json` contém `lucide-react` (grep -c "lucide-react" package.json retorna 1)
    - `package.json` NÃO contém `auth-helpers-nextjs` (grep -c "auth-helpers-nextjs" package.json retorna 0)
    - `.env.local.example` existe e contém `NEXT_PUBLIC_SUPABASE_URL`
    - `.gitignore` contém `.env.local`
  </acceptance_criteria>
  <done>Todas as deps da Fase 1 instaladas. .env.local.example criado como referência. Nenhuma dep deprecated presente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.local | Segredos do projeto — nunca commitados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information Disclosure) | .env.local | mitigate | .gitignore inclui .env.local; .env.local.example sem valores reais |
| T-01-02 | I (Information Disclosure) | NEXT_PUBLIC_ vars | accept | NEXT_PUBLIC_SUPABASE_URL e ANON_KEY são intencionalmente públicos (design do Supabase) |
</threat_model>

<verification>
```bash
cd c:/Users/homer/ccos-ratos/projetos/anja
npm run dev &
sleep 5
curl -s http://localhost:3000 | grep -c "Anja"
kill %1
```
</verification>

<success_criteria>
- npm run dev inicia sem erros
- npx next build completa sem erros
- Todas as deps listadas presentes no package.json
- .env.local.example existe como referência
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao/01-01-SUMMARY.md`
</output>
