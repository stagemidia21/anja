---
phase: 01-fundacao
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/middleware.ts
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
  - src/app/(auth)/layout.tsx
  - src/app/auth/callback/route.ts
  - src/app/(auth)/auth-form.tsx
autonomous: true
requirements: ["RF-01.1", "RF-01.2", "RF-01.3", "RF-01.4"]
must_haves:
  truths:
    - "Middleware protege /dashboard, /chat, /tarefas, /agenda — redireciona para /login sem auth"
    - "Middleware permite acesso a /login e /signup sem auth"
    - "Middleware chama getUser() em toda request para refresh de token"
    - "Pagina /login exibe botao Google OAuth e form email/senha"
    - "Pagina /signup exibe form de criacao de conta"
    - "Callback /auth/callback processa code e redireciona para /dashboard"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Middleware de proteção de rotas com refresh de token"
      contains: "getUser"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Página de login com Google OAuth + email/senha"
      contains: "Entrar com Google"
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "Página de signup com email/senha"
      contains: "Criar conta"
    - path: "src/app/auth/callback/route.ts"
      provides: "Callback para OAuth e email confirmation"
      contains: "exchangeCodeForSession"
    - path: "src/app/(auth)/layout.tsx"
      provides: "Layout das auth pages (centrado, grain)"
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/supabase/middleware.ts"
      via: "import updateSession"
      pattern: "updateSession"
    - from: "src/app/(auth)/login/page.tsx"
      to: "Supabase Auth"
      via: "signInWithOAuth e signInWithPassword"
      pattern: "signInWithOAuth"
    - from: "src/app/auth/callback/route.ts"
      to: "Supabase Auth"
      via: "exchangeCodeForSession"
      pattern: "exchangeCodeForSession"
---

<objective>
Implementar auth completa: middleware de proteção de rotas, páginas de login/signup com Google OAuth e email/senha, e callback de auth.

Purpose: Auth é gate para todo o app. Sem ela, nenhuma feature protegida funciona.
Output: Login com Google e email/senha funcionais, middleware protegendo rotas, sessão persistente com refresh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-fundacao/01-UI-SPEC.md
@.planning/phases/01-fundacao/01-02-SUMMARY.md
@.planning/phases/01-fundacao/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Componentes e utils criados nos planos anteriores -->

From src/components/ui/button.tsx:
```typescript
export function Button({ variant, loading, children, ...props }: ButtonProps)
// variant: 'primary' | 'secondary' | 'ghost'
```

From src/components/ui/input.tsx:
```typescript
export const Input = forwardRef<HTMLInputElement, InputProps>(...)
// Props: error?: boolean + InputHTMLAttributes
```

From src/lib/supabase/server.ts:
```typescript
export function createClient(): SupabaseClient
```

From src/lib/supabase/client.ts:
```typescript
export function createBrowserClient(): SupabaseClient
```

From src/lib/supabase/middleware.ts:
```typescript
export async function updateSession(request: NextRequest): Promise<{ supabaseResponse: NextResponse, user: User | null }>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Middleware de proteção de rotas</name>
  <files>src/middleware.ts</files>
  <read_first>
    .planning/research/stack-nextjs-supabase-claude.md (seção 2: middleware)
    src/lib/supabase/middleware.ts
  </read_first>
  <action>
Criar `src/middleware.ts` na raiz do src (NÃO na raiz do projeto — Next.js 14 com src-dir precisa do middleware no src/).

NOTA: Verificar se o Next.js 14 com `--src-dir` espera o middleware em `src/middleware.ts` ou `middleware.ts`. Se for na raiz do projeto, criar em `middleware.ts`. Testar e ajustar.

```typescript
import { updateSession } from '@/lib/supabase/middleware';
import { NextResponse, type NextRequest } from 'next/server';

// Rotas que requerem autenticação
const protectedRoutes = ['/dashboard', '/chat', '/tarefas', '/agenda'];

// Rotas de auth (redirecionar para dashboard se já logado)
const authRoutes = ['/login', '/signup'];

export async function middleware(request: NextRequest) {
  const { supabaseResponse, user } = await updateSession(request);
  const { pathname } = request.nextUrl;

  // Usuário NÃO autenticado tentando acessar rota protegida
  if (!user && protectedRoutes.some(route => pathname.startsWith(route))) {
    const loginUrl = request.nextUrl.clone();
    loginUrl.pathname = '/login';
    return NextResponse.redirect(loginUrl);
  }

  // Usuário autenticado tentando acessar páginas de auth
  if (user && authRoutes.some(route => pathname.startsWith(route))) {
    const dashboardUrl = request.nextUrl.clone();
    dashboardUrl.pathname = '/dashboard';
    return NextResponse.redirect(dashboardUrl);
  }

  return supabaseResponse;
}

export const config = {
  matcher: [
    /*
     * Match all request paths except:
     * - _next/static (static files)
     * - _next/image (image optimization files)
     * - favicon.ico (favicon file)
     * - public files (svg, png, jpg, etc)
     */
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
};
```

CRITICO:
- O middleware DEVE usar `updateSession` que chama `getUser()` internamente (refresh de token)
- NUNCA usar `getSession()` no middleware
- O matcher exclui arquivos estáticos para performance (research doc gotcha sobre latência)
  </action>
  <verify>
    <automated>cd c:/Users/homer/ccos-ratos/projetos/anja && (test -f src/middleware.ts && echo "SRC" || (test -f middleware.ts && echo "ROOT" || echo "MISSING")) && grep -c "updateSession" src/middleware.ts 2>/dev/null || grep -c "updateSession" middleware.ts 2>/dev/null && grep -r "getSession" src/middleware.ts middleware.ts 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - middleware.ts existe (em src/ ou raiz, conforme Next.js 14 com src-dir)
    - Importa e chama `updateSession` (grep -c "updateSession" retorna >= 1)
    - Contém array `protectedRoutes` com '/dashboard', '/chat', '/tarefas', '/agenda'
    - Contém array `authRoutes` com '/login', '/signup'
    - Redireciona para /login quando user é null em rota protegida
    - Redireciona para /dashboard quando user existe em rota de auth
    - ZERO ocorrências de `getSession` (grep -c "getSession" retorna 0)
    - matcher exclui _next/static e imagens
  </acceptance_criteria>
  <done>Middleware protege rotas autenticadas, redireciona auth pages quando logado, e renova token via getUser() em toda request.</done>
</task>

<task type="auto">
  <name>Task 2: Páginas de auth (login, signup, callback)</name>
  <files>src/app/(auth)/layout.tsx, src/app/(auth)/login/page.tsx, src/app/(auth)/signup/page.tsx, src/app/(auth)/auth-form.tsx, src/app/auth/callback/route.ts</files>
  <read_first>
    .planning/phases/01-fundacao/01-UI-SPEC.md (seção: Auth Pages, Copywriting Contract)
    src/components/ui/button.tsx
    src/components/ui/input.tsx
    src/lib/supabase/client.ts
    src/lib/supabase/server.ts
  </read_first>
  <action>
**1. Layout de auth `src/app/(auth)/layout.tsx`:**
```tsx
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="min-h-screen bg-charcoal flex items-center justify-center px-4">
      {children}
    </div>
  );
}
```

Note: grain class já está no body (layout.tsx raiz), não precisa repetir aqui.

**2. Componente compartilhado `src/app/(auth)/auth-form.tsx`:**
Client component ('use client') que contém toda a lógica de auth. Usado tanto em /login quanto /signup.

Props: `mode: 'login' | 'signup'`

Estrutura visual EXATA da UI-SPEC seção "Auth Pages":
- Card central: `w-full max-w-md bg-char-2 border border-char-3 rounded-xl p-8`
- Logo: "Anja" em `font-display text-gold text-4xl font-normal`
- Tagline: "Sua secretaria executiva com IA" em `text-sm text-muted mt-1`
- Separador: `border-t border-gold/20 my-6`
- Botão Google OAuth: "Entrar com Google" com ícone SVG inline do Google (NÃO lucide-react)
  - Estilo: `w-full flex items-center justify-center gap-3 px-4 py-3 rounded-lg bg-gold/10 border border-gold/20 text-cream text-sm font-normal hover:bg-gold/20 transition-colors duration-150`
  - Loading: mostra Spinner + "Entrando..."
- Divider "ou": `flex items-center gap-3 my-6` com linhas `flex-1 border-t border-char-3` e texto "ou" em `text-xs text-muted`
- Campos email e senha usando componente Input
  - Labels: `text-xs font-normal text-muted mb-1.5 block`
  - Espaçamento: `space-y-4`
- Botão submit: Button variant="primary" com `w-full`
  - /login: "Entrar na conta"
  - /signup: "Criar conta"
- Link de alternância:
  - /login: "Nao tem conta? Criar conta" (link para /signup)
  - /signup: "Ja tem conta? Entrar na conta" (link para /login)
  - Estilo: `text-sm text-muted` com link `text-gold hover:text-gold-light underline underline-offset-2`
- Mensagem de erro: quando auth falha, mostrar abaixo do botão submit em `text-sm text-danger mt-3`

Lógica de auth:
- Google OAuth: `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: window.location.origin + '/auth/callback' } })`
- Email login: `supabase.auth.signInWithPassword({ email, password })`
- Email signup: `supabase.auth.signUp({ email, password, options: { emailRedirectTo: window.location.origin + '/auth/callback' } })`

Tratamento de erros (copy da UI-SPEC):
- Credenciais inválidas: "E-mail ou senha incorretos."
- Google falhou: "Nao foi possivel conectar ao Google."
- Erro genérico: "Algo deu errado."

NÃO usar `font-medium` em nenhum lugar. Usar `font-normal` e `font-semibold` apenas.

**3. Página /login `src/app/(auth)/login/page.tsx`:**
```tsx
import { AuthForm } from '../auth-form';

export default function LoginPage() {
  return <AuthForm mode="login" />;
}
```

**4. Página /signup `src/app/(auth)/signup/page.tsx`:**
```tsx
import { AuthForm } from '../auth-form';

export default function SignupPage() {
  return <AuthForm mode="signup" />;
}
```

**5. Callback `src/app/auth/callback/route.ts`:**
Route handler que processa o code retornado pelo Supabase Auth (OAuth e email confirmation).

```typescript
import { createClient } from '@/lib/supabase/server';
import { NextResponse } from 'next/server';

export async function GET(request: Request) {
  const { searchParams, origin } = new URL(request.url);
  const code = searchParams.get('code');
  const next = searchParams.get('next') ?? '/dashboard';

  if (code) {
    const supabase = createClient();
    const { error } = await supabase.auth.exchangeCodeForSession(code);

    if (!error) {
      return NextResponse.redirect(`${origin}${next}`);
    }
  }

  // Erro ou code ausente — redirecionar para login com erro
  return NextResponse.redirect(`${origin}/login?error=auth_failed`);
}
```

NOTA: O callback fica em `src/app/auth/callback/route.ts` (SEM o group (auth)) porque é um route handler, não uma página.
  </action>
  <verify>
    <automated>cd c:/Users/homer/ccos-ratos/projetos/anja && test -f "src/app/(auth)/login/page.tsx" && test -f "src/app/(auth)/signup/page.tsx" && test -f "src/app/auth/callback/route.ts" && test -f "src/app/(auth)/auth-form.tsx" && echo "ALL FILES EXIST" && grep -c "Entrar com Google" "src/app/(auth)/auth-form.tsx" && grep -c "signInWithOAuth" "src/app/(auth)/auth-form.tsx" && grep -c "exchangeCodeForSession" src/app/auth/callback/route.ts && grep -r "font-medium" "src/app/(auth)/" | wc -l && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(auth)/login/page.tsx` existe e renderiza AuthForm com mode="login"
    - `src/app/(auth)/signup/page.tsx` existe e renderiza AuthForm com mode="signup"
    - `src/app/(auth)/auth-form.tsx` existe com 'use client'
    - `auth-form.tsx` contém "Entrar com Google" (grep -c retorna 1)
    - `auth-form.tsx` contém `signInWithOAuth` para Google (grep -c retorna >= 1)
    - `auth-form.tsx` contém `signInWithPassword` para email login (grep -c retorna >= 1)
    - `auth-form.tsx` contém `signUp` para email signup (grep -c retorna >= 1)
    - `auth-form.tsx` usa componentes Button e Input de `@/components/ui/`
    - `src/app/auth/callback/route.ts` existe e usa `exchangeCodeForSession`
    - `src/app/(auth)/layout.tsx` existe com layout centrado
    - ZERO ocorrências de `font-medium` em arquivos de auth
    - ZERO ocorrências de `getSession` em arquivos de auth
    - `npx next build` completa sem erros
  </acceptance_criteria>
  <done>Páginas /login e /signup com Google OAuth + email/senha, callback de auth, e layout centrado premium. Copy em PT-BR conforme UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase Auth | Login credentials e OAuth flow |
| Supabase Auth → callback route | Authorization code exchange |
| middleware → Supabase Auth | Token validation via getUser() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | S (Spoofing) | Auth bypass | mitigate | Middleware chama getUser() em TODA request (valida com servidor Supabase). Nunca usa getSession(). |
| T-01-04-02 | S (Spoofing) | OAuth CSRF | mitigate | Supabase Auth usa PKCE flow internamente. O code no callback é single-use e tied ao browser session. |
| T-01-04-03 | T (Tampering) | Callback redirect | mitigate | Callback valida code via exchangeCodeForSession. Redirect usa origin do request (não input externo). next param limitado a paths internos. |
| T-01-04-04 | I (Information Disclosure) | Error messages | mitigate | Mensagens genéricas ("E-mail ou senha incorretos") sem detalhes técnicos. Sem stack traces. |
| T-01-04-05 | R (Repudiation) | Login attempts | accept | Supabase Auth tem rate limiting built-in para login attempts. Suficiente para MVP. |
| T-01-04-06 | E (Elevation of Privilege) | Middleware bypass | mitigate | Matcher cobre todas as rotas exceto static assets. protectedRoutes lista explícita. |
</threat_model>

<verification>
```bash
cd c:/Users/homer/ccos-ratos/projetos/anja
# Middleware existe e usa updateSession
grep -c "updateSession" src/middleware.ts 2>/dev/null || grep -c "updateSession" middleware.ts
# Auth pages existem
ls "src/app/(auth)/login/page.tsx" "src/app/(auth)/signup/page.tsx"
# Callback existe
ls src/app/auth/callback/route.ts
# Zero getSession
grep -r "getSession" src/ | wc -l  # 0
# Build
npx next build
```
</verification>

<success_criteria>
- Middleware protege rotas e renova tokens via getUser()
- /login mostra Google OAuth + email/senha
- /signup mostra form de criação de conta
- /auth/callback processa code e redireciona para /dashboard
- Copy em PT-BR conforme UI-SPEC
- Zero font-medium, zero getSession
- Build passa
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao/01-04-SUMMARY.md`
</output>
