---
phase: 01-fundacao
plan: 05
type: execute
wave: 4
depends_on: ["01-02", "01-04"]
files_modified:
  - src/app/(dashboard)/layout.tsx
  - src/components/layout/sidebar.tsx
  - src/components/layout/sidebar-item.tsx
  - src/components/layout/bottom-nav.tsx
  - src/components/layout/header.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/chat/page.tsx
  - src/app/(dashboard)/tarefas/page.tsx
  - src/app/(dashboard)/agenda/page.tsx
  - src/app/page.tsx
autonomous: false
requirements: ["RNF-02"]
must_haves:
  truths:
    - "Sidebar desktop mostra 4 nav items: Dashboard, Chat, Tarefas, Agenda"
    - "Sidebar colapsa para icones em md (768px) e expande com labels em lg (1024px)"
    - "Sidebar oculta em mobile (<768px)"
    - "Bottom nav aparece em mobile com 4 items"
    - "Bottom nav usa env(safe-area-inset-bottom) para iOS"
    - "Header mostra titulo da pagina e avatar do usuario"
    - "Paginas placeholder existem para /dashboard, /chat, /tarefas, /agenda"
    - "/ redireciona para /dashboard"
  artifacts:
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Layout do dashboard com sidebar + header + bottom nav"
      contains: "Sidebar"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Sidebar desktop com nav items"
      contains: "SidebarItem"
    - path: "src/components/layout/sidebar-item.tsx"
      provides: "Item individual da sidebar com estado ativo"
      exports: ["SidebarItem"]
    - path: "src/components/layout/bottom-nav.tsx"
      provides: "Bottom nav mobile com 4 items"
      contains: "safe-area-pb"
    - path: "src/components/layout/header.tsx"
      provides: "Header com titulo e avatar"
      contains: "font-semibold"
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Pagina placeholder do dashboard"
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/layout/sidebar.tsx"
      via: "import Sidebar"
      pattern: "Sidebar"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/layout/bottom-nav.tsx"
      via: "import BottomNav"
      pattern: "BottomNav"
    - from: "src/components/layout/sidebar.tsx"
      to: "src/lib/supabase/server.ts"
      via: "getUser() para dados do usuario"
      pattern: "getUser"
---

<objective>
Implementar o layout completo do dashboard: sidebar desktop (collapsed/expanded), bottom nav mobile com safe-area, header com titulo e avatar, e paginas placeholder.

Purpose: Estrutura de navegacao do app. Define como o usuario se move entre features.
Output: Layout responsivo funcional com sidebar/bottom nav e 4 paginas placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-fundacao/01-UI-SPEC.md
@.planning/phases/01-fundacao/01-02-SUMMARY.md
@.planning/phases/01-fundacao/01-04-SUMMARY.md
</context>

<interfaces>
<!-- Componentes criados nos planos anteriores -->

From src/components/ui/badge.tsx:
```typescript
export function Badge({ variant, children }: BadgeProps)
// variant: 'free' | 'essencial' | 'executivo' | 'agencias' | 'default'
```

From src/lib/supabase/server.ts:
```typescript
export function createClient(): SupabaseClient
// Usar em Server Components para pegar dados do usuario
```

CSS variables disponíveis:
- bg-charcoal, bg-char-2, bg-char-3 (backgrounds)
- text-cream, text-muted, text-gold, text-gold-light (texto)
- border-char-3, border-gold/20 (bordas)
- font-display (Cormorant), font-body (DM Sans)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar, Bottom Nav e Header</name>
  <files>src/components/layout/sidebar.tsx, src/components/layout/sidebar-item.tsx, src/components/layout/bottom-nav.tsx, src/components/layout/header.tsx</files>
  <read_first>
    .planning/phases/01-fundacao/01-UI-SPEC.md (seções: Sidebar, Bottom Nav, Header — ler com atenção os estilos exatos)
    src/components/ui/badge.tsx
    src/lib/supabase/server.ts
  </read_first>
  <action>
Criar diretório `src/components/layout/` e implementar 4 componentes.

**`src/components/layout/sidebar-item.tsx`** ('use client'):
- Props: `icon: React.ElementType`, `label: string`, `href: string`, `active: boolean`
- Usa `next/link`
- Estilos EXATOS da UI-SPEC:
  - Container: `flex items-center gap-3 px-3 py-2.5 rounded-lg transition-colors duration-150`
  - Inativo: `text-muted hover:text-cream hover:bg-char-3`
  - Ativo: `bg-gold/10 text-gold border border-gold/20`
  - Icone: `w-5 h-5 shrink-0`
  - Label: `text-sm font-normal hidden lg:block`
- ACESSIBILIDADE: quando label está oculta (sidebar collapsed), incluir `aria-label={label}` no Link

**`src/components/layout/sidebar.tsx`** ('use client'):
- Props: `user: { name: string; plan: string } | null`
- Usa `usePathname()` de `next/navigation` para determinar item ativo
- Estilos EXATOS:
  - Container: `hidden md:flex flex-col bg-char-2 border-r border-char-3 sticky top-0 h-screen w-16 lg:w-56 transition-all duration-200`
  - Logo topo: padding `px-4 py-6`, separador `border-b border-char-3`
    - "Anja" em `font-display text-gold text-xl font-normal hidden lg:block`
    - Versão collapsed: "A" em `font-display text-gold text-xl lg:hidden`
  - Nav items container: `flex-1 py-4 space-y-1 px-2`
  - 4 items usando lucide-react icons:
    1. Dashboard — `LayoutDashboard` — `/dashboard`
    2. Chat (Anja) — `MessageSquare` — `/chat`
    3. Tarefas — `CheckSquare` — `/tarefas`
    4. Agenda — `Calendar` — `/agenda`
  - User info (base): `px-3 py-4 border-t border-char-3`
    - Avatar: `w-8 h-8 rounded-full bg-gold/20 flex items-center justify-center`
    - Inicial: `text-sm font-normal text-gold` (primeira letra do nome)
    - Nome: `text-sm font-normal text-cream hidden lg:block truncate`
    - Badge de plano usando componente Badge (variant baseada no plano)
    - Textos ocultos em collapsed: `hidden lg:block`

**`src/components/layout/bottom-nav.tsx`** ('use client'):
- Usa `usePathname()` para item ativo
- Estilos EXATOS:
  - Container: `fixed bottom-0 left-0 right-0 z-50 bg-char-2 border-t border-char-3 flex items-center justify-around px-2 safe-area-pb md:hidden`
  - Cada item: `flex flex-col items-center gap-1 px-3 py-1.5 rounded-lg`
    - Icone: `w-5 h-5`
    - Label: `text-[10px] font-normal`
    - Inativo: `text-muted`
    - Ativo: `text-gold`
  - 4 items (mesmos da sidebar): Dashboard, Chat, Tarefas, Agenda

**`src/components/layout/header.tsx`:**
- Props: `title: string`, `user: { name: string; plan: string } | null`
- Estilos EXATOS:
  - Container: `sticky top-0 z-40 bg-char-2 border-b border-char-3 flex items-center justify-between px-4 md:px-6 py-3 h-14`
  - Titulo esquerda: `text-xl font-semibold text-cream`
  - Direita: avatar + badge de plano
    - Avatar: `w-8 h-8 rounded-full bg-gold/20 flex items-center justify-center`
    - Inicial: `text-sm font-normal text-gold`
    - Badge: `hidden md:inline-flex` (oculto no mobile)

IMPORTANTE:
- NÃO usar `font-medium` (500) — apenas `font-normal` (400) e `font-semibold` (600)
- Sidebar usa React state + Tailwind breakpoints (SEM biblioteca externa para sidebar)
- Bottom nav usa classe `safe-area-pb` definida no globals.css
- Todos os items usam `next/link` para navegação
  </action>
  <verify>
    <automated>cd c:/Users/homer/ccos-ratos/projetos/anja && for f in sidebar sidebar-item bottom-nav header; do test -f "src/components/layout/$f.tsx" && echo "$f OK" || echo "$f MISSING"; done && grep -c "aria-label" src/components/layout/sidebar-item.tsx && grep -c "safe-area-pb" src/components/layout/bottom-nav.tsx && grep -r "font-medium" src/components/layout/ | wc -l && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 4 arquivos existem: sidebar.tsx, sidebar-item.tsx, bottom-nav.tsx, header.tsx em src/components/layout/
    - `sidebar.tsx` contém `hidden md:flex` (oculta no mobile)
    - `sidebar.tsx` contém `w-16 lg:w-56` (collapsed/expanded)
    - `sidebar.tsx` contém "Anja" em font-display text-gold
    - `sidebar-item.tsx` contém `aria-label` para acessibilidade
    - `sidebar-item.tsx` contém `bg-gold/10 text-gold` para estado ativo
    - `bottom-nav.tsx` contém `safe-area-pb` (iOS home indicator)
    - `bottom-nav.tsx` contém `md:hidden` (oculto no desktop)
    - `bottom-nav.tsx` contém `fixed bottom-0`
    - `header.tsx` contém `h-14` (56px)
    - `header.tsx` contém `font-semibold` no titulo
    - ZERO ocorrencias de `font-medium` (grep -r "font-medium" src/components/layout/ retorna vazio)
    - Todos usam `usePathname` de next/navigation
    - `npx next build` completa sem erros
  </acceptance_criteria>
  <done>Sidebar (collapsed/expanded), bottom nav (mobile com safe-area), header com titulo e avatar. Navegacao com 4 items. Acessibilidade com aria-label.</done>
</task>

<task type="auto">
  <name>Task 2: Layout dashboard e paginas placeholder</name>
  <files>src/app/(dashboard)/layout.tsx, src/app/(dashboard)/dashboard/page.tsx, src/app/(dashboard)/chat/page.tsx, src/app/(dashboard)/tarefas/page.tsx, src/app/(dashboard)/agenda/page.tsx, src/app/page.tsx</files>
  <read_first>
    .planning/phases/01-fundacao/01-UI-SPEC.md (seção: AppShell layout)
    src/components/layout/sidebar.tsx
    src/components/layout/bottom-nav.tsx
    src/components/layout/header.tsx
    src/lib/supabase/server.ts
  </read_first>
  <action>
**1. Layout do dashboard `src/app/(dashboard)/layout.tsx`:**
Server component que busca dados do usuário e monta o AppShell.

```tsx
import { createClient } from '@/lib/supabase/server';
import { redirect } from 'next/navigation';
import { Sidebar } from '@/components/layout/sidebar';
import { BottomNav } from '@/components/layout/bottom-nav';
import { Header } from '@/components/layout/header';

export default async function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const supabase = createClient();
  const { data: { user } } = await supabase.auth.getUser();

  if (!user) {
    redirect('/login');
  }

  // Buscar profile e plano
  const { data: profile } = await supabase
    .from('profiles')
    .select('full_name')
    .eq('id', user.id)
    .single();

  const { data: userPlan } = await supabase
    .from('user_plans')
    .select('plan')
    .eq('user_id', user.id)
    .single();

  const userData = {
    name: profile?.full_name || user.email?.split('@')[0] || 'Usuario',
    plan: userPlan?.plan || 'free',
  };

  return (
    <div className="flex min-h-screen">
      <Sidebar user={userData} />
      <div className="flex-1 flex flex-col min-w-0">
        <Header title="Dashboard" user={userData} />
        <main className="flex-1 p-4 md:p-6 pb-20 md:pb-6">
          {children}
        </main>
      </div>
      <BottomNav />
    </div>
  );
}
```

Layout EXATO da UI-SPEC:
- `flex` horizontal: Sidebar esquerda + conteúdo direita
- `<main>`: `pb-20 md:pb-6` (espaço para bottom nav no mobile — 16 = 64px + padding)
- Background: `bg-charcoal` (já no body via layout raiz)

NOTA: O Header recebe `title` fixo aqui. Em fases futuras, cada página pode sobrescrever via metadata ou context. Para esta fase, "Dashboard" é suficiente pois o titulo é da página, não dinâmico.

Para resolver o titulo dinâmico por página, usar um approach simples: cada página wrapper pode setar o titulo. MAS para manter simples na Fase 1, o Header pode receber titulo via props ou context. A implementação mais simples: cada página passa o título como parte do children ou via um layout nested.

APPROACH RECOMENDADO: Usar o pathname no Header para determinar o título automaticamente:
```tsx
// No header.tsx, usar usePathname e um map:
const titles: Record<string, string> = {
  '/dashboard': 'Dashboard',
  '/chat': 'Chat',
  '/tarefas': 'Tarefas',
  '/agenda': 'Agenda',
};
```

Neste caso, Header não precisa de prop `title` — ele lê o pathname. Ajustar header.tsx para usar `usePathname()` com o map acima. Header vira client component ('use client') com props apenas de `user`.

**2. Páginas placeholder:**

Cada página é um Server Component simples:

`src/app/(dashboard)/dashboard/page.tsx`:
```tsx
export default function DashboardPage() {
  return (
    <div className="space-y-4">
      <p className="text-muted text-sm">Dashboard em construcao — conteudo chega na Fase 5.</p>
    </div>
  );
}
```

`src/app/(dashboard)/chat/page.tsx`:
```tsx
export default function ChatPage() {
  return (
    <div className="space-y-4">
      <p className="text-muted text-sm">Chat com a Anja — disponivel na Fase 2.</p>
    </div>
  );
}
```

`src/app/(dashboard)/tarefas/page.tsx`:
```tsx
export default function TarefasPage() {
  return (
    <div className="space-y-4">
      <p className="text-muted text-sm">Gestao de tarefas — disponivel na Fase 4.</p>
    </div>
  );
}
```

`src/app/(dashboard)/agenda/page.tsx`:
```tsx
export default function AgendaPage() {
  return (
    <div className="space-y-4">
      <p className="text-muted text-sm">Agenda — disponivel na Fase 3.</p>
    </div>
  );
}
```

**3. Redirect da home `src/app/page.tsx`:**
```tsx
import { redirect } from 'next/navigation';

export default function Home() {
  redirect('/dashboard');
}
```

IMPORTANTE:
- `getUser()` no layout (NUNCA getSession)
- `pb-20 md:pb-6` no main (espaço para bottom nav mobile)
- NÃO usar `font-medium` em nenhum lugar
  </action>
  <verify>
    <automated>cd c:/Users/homer/ccos-ratos/projetos/anja && test -f "src/app/(dashboard)/layout.tsx" && test -f "src/app/(dashboard)/dashboard/page.tsx" && test -f "src/app/(dashboard)/chat/page.tsx" && test -f "src/app/(dashboard)/tarefas/page.tsx" && test -f "src/app/(dashboard)/agenda/page.tsx" && echo "ALL PAGES EXIST" && grep -c "getUser" "src/app/(dashboard)/layout.tsx" && grep -c "redirect" src/app/page.tsx && grep -r "font-medium" "src/app/(dashboard)/" | wc -l && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(dashboard)/layout.tsx` existe e importa Sidebar, BottomNav, Header
    - Layout busca user via `getUser()` (grep -c "getUser" retorna >= 1)
    - Layout busca profile e user_plans do Supabase
    - Layout redireciona para /login se user é null
    - `<main>` contém padding-bottom para bottom nav (grep -c "pb-20" ou "pb-16" retorna >= 1)
    - 4 paginas placeholder existem: dashboard, chat, tarefas, agenda
    - `src/app/page.tsx` redireciona para /dashboard
    - ZERO ocorrencias de `getSession` (grep -r "getSession" "src/app/(dashboard)/" retorna vazio)
    - ZERO ocorrencias de `font-medium` (grep -r "font-medium" "src/app/(dashboard)/" retorna vazio)
    - `npx next build` completa sem erros
  </acceptance_criteria>
  <done>Layout AppShell com sidebar + header + bottom nav. 4 paginas placeholder. Home redireciona para /dashboard. User data carregado do Supabase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacao visual do layout completo</name>
  <what-built>Layout responsivo do dashboard com sidebar desktop (collapsed em 768px+, expanded em 1024px+), bottom nav mobile, header, e grain texture sobre todo o app. Páginas de auth (/login, /signup) com design premium.</what-built>
  <how-to-verify>
    1. Abrir http://localhost:3000 — deve redirecionar para /login
    2. Na pagina de login, verificar:
       - Logo "Anja" em dourado (Cormorant Garamond)
       - Tagline "Sua secretaria executiva com IA"
       - Botao "Entrar com Google"
       - Campos email/senha
       - Grain texture sutil no background
    3. Redimensionar a janela para mobile (<768px) — layout deve se adaptar
    4. Se possivel fazer login (com Supabase configurado):
       - Sidebar deve aparecer no desktop com 4 items
       - Bottom nav deve aparecer no mobile
       - Header deve mostrar titulo e avatar
    5. Verificar que nao ha texto com peso "medium" (tudo deve ser regular ou semibold)
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component → Supabase | Layout busca dados do usuario autenticado |
| Client Component → pathname | usePathname para estado ativo (confiavel) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | I (Information Disclosure) | Layout Supabase query | mitigate | Query usa RLS (auth.uid() = id). Dados do usuario nunca vazam para outros usuarios. |
| T-01-05-02 | S (Spoofing) | Layout auth check | mitigate | Layout chama getUser() e redireciona para /login se null. Double-check alem do middleware. |
</threat_model>

<verification>
```bash
cd c:/Users/homer/ccos-ratos/projetos/anja
# Layout existe
ls "src/app/(dashboard)/layout.tsx"
# Componentes existem
ls src/components/layout/*.tsx | wc -l  # 4
# Paginas existem
ls "src/app/(dashboard)"/*/page.tsx | wc -l  # 4
# Home redirect
grep -c "redirect" src/app/page.tsx  # 1
# Zero font-medium no projeto inteiro
grep -r "font-medium" src/ | wc -l  # 0
# Build
npx next build
```
</verification>

<success_criteria>
- Layout responsivo: sidebar desktop + bottom nav mobile
- Sidebar: collapsed (w-16) em md, expanded (w-56) em lg
- Bottom nav: fixed bottom com safe-area-pb, oculto em md+
- Header: titulo dinamico, avatar, badge de plano
- 4 paginas placeholder funcionam
- / redireciona para /dashboard
- Nenhum font-medium no projeto
- Build passa
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao/01-05-SUMMARY.md`
</output>
