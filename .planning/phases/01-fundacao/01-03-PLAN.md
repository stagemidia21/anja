---
phase: 01-fundacao
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - supabase/migrations/00001_initial_schema.sql
  - src/lib/supabase/server.ts
  - src/lib/supabase/client.ts
  - src/lib/supabase/middleware.ts
autonomous: true
requirements: ["RNF-03"]
must_haves:
  truths:
    - "Tabelas profiles, oauth_tokens, conversations, messages, tasks, usage_monthly e user_plans existem no Supabase"
    - "RLS está habilitado em todas as tabelas com policies por auth.uid()"
    - "Função check_and_increment_usage existe como RPC"
    - "Clientes Supabase para server, client e middleware estão implementados"
  artifacts:
    - path: "supabase/migrations/00001_initial_schema.sql"
      provides: "Schema completo com RLS e functions"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "src/lib/supabase/server.ts"
      provides: "createClient para Server Components e Route Handlers"
      exports: ["createClient"]
    - path: "src/lib/supabase/client.ts"
      provides: "createBrowserClient para Client Components"
      exports: ["createBrowserClient"]
    - path: "src/lib/supabase/middleware.ts"
      provides: "updateSession para middleware de auth"
      exports: ["updateSession"]
  key_links:
    - from: "src/lib/supabase/server.ts"
      to: "@supabase/ssr"
      via: "createServerClient import"
      pattern: "createServerClient"
    - from: "src/lib/supabase/middleware.ts"
      to: "supabase.auth.getUser()"
      via: "Chamada obrigatoria para refresh de token"
      pattern: "getUser\\(\\)"
---

<objective>
Criar o schema SQL completo do Supabase com todas as tabelas, RLS policies e functions necessárias para o MVP. Implementar os 3 clientes Supabase (@supabase/ssr).

Purpose: Banco de dados e segurança são pré-requisitos para auth, chat e todas as features.
Output: Migration SQL pronta para aplicar + clientes Supabase tipados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/stack-nextjs-supabase-claude.md
@.planning/phases/01-fundacao/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema SQL completo com RLS e functions</name>
  <files>supabase/migrations/00001_initial_schema.sql</files>
  <read_first>
    .planning/REQUIREMENTS.md (todas as tabelas mencionadas)
    .planning/research/stack-nextjs-supabase-claude.md (seções: rate limiting, oauth_tokens, RLS)
  </read_first>
  <action>
Criar diretório `supabase/migrations/` e o arquivo de migration.

O SQL deve criar TODAS as tabelas do MVP de uma vez (este é o schema inicial):

**1. profiles** (extends auth.users):
```sql
CREATE TABLE profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  full_name TEXT,
  avatar_url TEXT,
  custom_name TEXT,          -- whitelabel (RF-08)
  custom_logo_url TEXT,      -- whitelabel (RF-08)
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

**2. user_plans**:
```sql
CREATE TABLE user_plans (
  user_id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  plan TEXT NOT NULL DEFAULT 'free' CHECK (plan IN ('free', 'essencial', 'executivo', 'agencias')),
  message_limit INTEGER NOT NULL DEFAULT 30,
  task_limit INTEGER NOT NULL DEFAULT 10,
  stripe_customer_id TEXT,
  stripe_subscription_id TEXT,
  valid_until TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

**3. oauth_tokens**:
```sql
CREATE TABLE oauth_tokens (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  provider TEXT NOT NULL DEFAULT 'google',
  access_token TEXT NOT NULL,
  refresh_token TEXT,
  expires_at TIMESTAMPTZ NOT NULL,
  scope TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(user_id, provider)
);
```

**4. conversations**:
```sql
CREATE TABLE conversations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  title TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

**5. messages**:
```sql
CREATE TABLE messages (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  conversation_id UUID REFERENCES conversations(id) ON DELETE CASCADE NOT NULL,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  role TEXT NOT NULL CHECK (role IN ('user', 'assistant', 'system', 'tool')),
  content TEXT NOT NULL,
  tool_invocations JSONB,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

**6. tasks**:
```sql
CREATE TABLE tasks (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  title TEXT NOT NULL,
  note TEXT,
  priority TEXT NOT NULL DEFAULT 'media' CHECK (priority IN ('alta', 'media', 'baixa')),
  category TEXT,
  status TEXT NOT NULL DEFAULT 'fazer' CHECK (status IN ('fazer', 'fazendo', 'feito')),
  due_date DATE,
  position INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

**7. usage_monthly**:
```sql
CREATE TABLE usage_monthly (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  year_month TEXT NOT NULL,
  message_count INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(user_id, year_month)
);
```

**RLS — HABILITAR EM TODAS AS TABELAS com policies por auth.uid():**

Para CADA tabela:
```sql
ALTER TABLE {table_name} ENABLE ROW LEVEL SECURITY;

CREATE POLICY "{table_name}_select" ON {table_name}
  FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "{table_name}_insert" ON {table_name}
  FOR INSERT WITH CHECK (auth.uid() = user_id);

CREATE POLICY "{table_name}_update" ON {table_name}
  FOR UPDATE USING (auth.uid() = user_id);

CREATE POLICY "{table_name}_delete" ON {table_name}
  FOR DELETE USING (auth.uid() = user_id);
```

Para `profiles`: usar `auth.uid() = id` (não user_id).

Para `messages`: policy de SELECT e INSERT usa `auth.uid() = user_id`. Usuário não deve poder UPDATE ou DELETE mensagens (append-only).

**Function check_and_increment_usage** (atômica com FOR UPDATE):
Implementar EXATAMENTE como documentado na research (stack-nextjs-supabase-claude.md seção 4), mas com os limites corretos do REQUIREMENTS.md:
- free: 30 mensagens/mês
- essencial: 200 mensagens/mês
- executivo: -1 (ilimitado)
- agencias: -1 (ilimitado)

A função deve ser `SECURITY DEFINER` e incluir verificação `auth.uid() = p_user_id` para segurança.

**Trigger para criar profile e user_plan automaticamente no signup:**
```sql
CREATE OR REPLACE FUNCTION handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO profiles (id, full_name, avatar_url)
  VALUES (
    NEW.id,
    COALESCE(NEW.raw_user_meta_data->>'full_name', NEW.raw_user_meta_data->>'name', ''),
    COALESCE(NEW.raw_user_meta_data->>'avatar_url', NEW.raw_user_meta_data->>'picture', '')
  );

  INSERT INTO user_plans (user_id, plan, message_limit, task_limit)
  VALUES (NEW.id, 'free', 30, 10);

  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION handle_new_user();
```

**Indexes para performance:**
```sql
CREATE INDEX idx_messages_conversation ON messages(conversation_id, created_at);
CREATE INDEX idx_tasks_user_status ON tasks(user_id, status);
CREATE INDEX idx_usage_monthly_user ON usage_monthly(user_id, year_month);
```
  </action>
  <verify>
    <automated>cd c:/Users/homer/ccos-ratos/projetos/anja && test -f supabase/migrations/00001_initial_schema.sql && echo "FILE EXISTS" && grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/00001_initial_schema.sql && grep -c "check_and_increment_usage" supabase/migrations/00001_initial_schema.sql && grep -c "handle_new_user" supabase/migrations/00001_initial_schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/00001_initial_schema.sql` existe
    - Contém CREATE TABLE para 7 tabelas: profiles, user_plans, oauth_tokens, conversations, messages, tasks, usage_monthly (grep -c "CREATE TABLE" retorna 7)
    - Contém ENABLE ROW LEVEL SECURITY para todas as 7 tabelas (grep -c "ENABLE ROW LEVEL SECURITY" retorna 7)
    - Contém function check_and_increment_usage com FOR UPDATE (grep -c "FOR UPDATE" retorna >= 1)
    - Contém function handle_new_user com trigger on_auth_user_created
    - Contém CHECK constraint para plans: 'free', 'essencial', 'executivo', 'agencias'
    - Contém CHECK constraint para task status: 'fazer', 'fazendo', 'feito'
    - Contém CHECK constraint para message role: 'user', 'assistant', 'system', 'tool'
    - Contém indexes (grep -c "CREATE INDEX" retorna >= 3)
  </acceptance_criteria>
  <done>Schema SQL completo com 7 tabelas, RLS em todas, function de rate limiting atômica, trigger de auto-criação de profile/plan, e indexes de performance.</done>
</task>

<task type="auto">
  <name>Task 2: Clientes Supabase (server, client, middleware)</name>
  <files>src/lib/supabase/server.ts, src/lib/supabase/client.ts, src/lib/supabase/middleware.ts</files>
  <read_first>
    .planning/research/stack-nextjs-supabase-claude.md (seção 2: Supabase SSR Auth — três clientes)
  </read_first>
  <action>
Criar diretório `src/lib/supabase/` com 3 arquivos seguindo EXATAMENTE os padrões da research doc.

**`src/lib/supabase/server.ts`** — para Server Components e Route Handlers:
```typescript
import { createServerClient } from '@supabase/ssr';
import { cookies } from 'next/headers';

export function createClient() {
  const cookieStore = cookies();

  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll();
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            );
          } catch {
            // Server Components não podem setar cookies — ignorar
          }
        },
      },
    }
  );
}
```

**`src/lib/supabase/client.ts`** — para Client Components:
```typescript
'use client';

import { createBrowserClient } from '@supabase/ssr';

export function createBrowserClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  );
}
```

NOTA: Usar nome de função `createBrowserClient` que não conflita com o import. Renomear o import se necessário:
```typescript
import { createBrowserClient as createSupabaseBrowserClient } from '@supabase/ssr';

export function createBrowserClient() {
  return createSupabaseBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  );
}
```

**`src/lib/supabase/middleware.ts`** — para o middleware de auth:
```typescript
import { createServerClient } from '@supabase/ssr';
import { NextResponse, type NextRequest } from 'next/server';

export async function updateSession(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request });

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll();
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) =>
            request.cookies.set(name, value)
          );
          supabaseResponse = NextResponse.next({ request });
          cookiesToSet.forEach(({ name, value, options }) =>
            supabaseResponse.cookies.set(name, value, options)
          );
        },
      },
    }
  );

  // CRITICO: getUser() renova o token se necessario
  // NUNCA usar getSession() para verificacao de auth — nao valida com o servidor
  const { data: { user } } = await supabase.auth.getUser();

  return { supabaseResponse, user };
}
```

IMPORTANTE:
- Usar SEMPRE `getUser()`, NUNCA `getSession()` para verificação de auth
- O middleware.ts retorna `{ supabaseResponse, user }` para que o middleware principal possa decidir redirect baseado no user
- NÃO usar `@supabase/auth-helpers-nextjs` (deprecated)
  </action>
  <verify>
    <automated>cd c:/Users/homer/ccos-ratos/projetos/anja && grep -c "createServerClient" src/lib/supabase/server.ts && grep -c "createBrowserClient\|createSupabaseBrowserClient" src/lib/supabase/client.ts && grep -c "getUser" src/lib/supabase/middleware.ts && grep -r "getSession" src/lib/supabase/ | wc -l && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/supabase/server.ts` existe e importa `createServerClient` de `@supabase/ssr`
    - `src/lib/supabase/client.ts` existe e importa `createBrowserClient` de `@supabase/ssr`
    - `src/lib/supabase/middleware.ts` existe e chama `getUser()` (grep -c "getUser" retorna >= 1)
    - ZERO ocorrencias de `getSession` em qualquer arquivo supabase (grep -r "getSession" src/lib/supabase/ retorna vazio)
    - ZERO ocorrencias de `auth-helpers-nextjs` (grep -r "auth-helpers" src/ retorna vazio)
    - `npx next build` completa sem erros
  </acceptance_criteria>
  <done>3 clientes Supabase implementados: server (Server Components), client (Client Components), middleware (token refresh). Usando getUser() exclusivamente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase | anon key é público; RLS protege dados |
| server → Supabase | server.ts usa anon key com cookies do usuário |
| auth.users → profiles | trigger automático; SECURITY DEFINER |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | E (Elevation of Privilege) | RLS bypass | mitigate | RLS habilitado em TODAS as 7 tabelas. Policies restringem por auth.uid(). |
| T-01-03-02 | I (Information Disclosure) | oauth_tokens | mitigate | RLS policy restringe acesso a tokens por auth.uid() = user_id. service_role nunca exposto ao client. |
| T-01-03-03 | T (Tampering) | check_and_increment_usage | mitigate | SECURITY DEFINER com verificação auth.uid() = p_user_id dentro da function. FOR UPDATE previne race condition. |
| T-01-03-04 | S (Spoofing) | handle_new_user trigger | accept | Trigger executa apenas em INSERT na auth.users (controlada pelo Supabase Auth). Não aceita input externo. |
| T-01-03-05 | I (Information Disclosure) | NEXT_PUBLIC_ env vars | accept | SUPABASE_URL e ANON_KEY são intencionalmente públicos (design do Supabase). RLS é a camada de segurança. |
| T-01-03-06 | T (Tampering) | SQL injection via RPC | mitigate | Função usa parâmetros tipados ($1, $2), não concatenação de strings. PostgreSQL previne injection em PL/pgSQL. |
</threat_model>

<verification>
```bash
cd c:/Users/homer/ccos-ratos/projetos/anja
# Verificar schema
grep -c "CREATE TABLE" supabase/migrations/00001_initial_schema.sql  # 7
grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/00001_initial_schema.sql  # 7
# Verificar clients
grep -c "getUser" src/lib/supabase/middleware.ts  # >= 1
grep -r "getSession" src/lib/supabase/ | wc -l  # 0
# Build
npx next build
```
</verification>

<success_criteria>
- Schema SQL com 7 tabelas, RLS em todas, function de rate limiting, trigger de signup
- 3 clientes Supabase implementados corretamente
- getUser() usado exclusivamente (nunca getSession)
- Build passa sem erros
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao/01-03-SUMMARY.md`
</output>
