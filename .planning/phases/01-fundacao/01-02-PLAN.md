---
phase: 01-fundacao
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/globals.css
  - tailwind.config.ts
  - src/app/layout.tsx
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/spinner.tsx
autonomous: true
requirements: ["RNF-04"]
must_haves:
  truths:
    - "CSS variables RGB definidas em globals.css e classes Tailwind como bg-charcoal funcionam"
    - "Grain texture visível sobre o background"
    - "Fontes Cormorant Garamond, DM Sans e JetBrains Mono carregam via next/font/google"
    - "Componentes Button, Input, Card, Badge e Spinner existem e exportam corretamente"
    - "Nenhuma classe font-medium (peso 500) existe no código"
  artifacts:
    - path: "src/app/globals.css"
      provides: "CSS variables RGB + grain texture + safe-area utility"
      contains: "--charcoal"
    - path: "tailwind.config.ts"
      provides: "Cores customizadas mapeadas para CSS variables"
      contains: "rgb(var(--charcoal)"
    - path: "src/app/layout.tsx"
      provides: "Root layout com fontes next/font/google + classe grain no body"
      contains: "font-cormorant"
    - path: "src/components/ui/button.tsx"
      provides: "Componente Button com 3 variantes (primary, secondary, ghost)"
      exports: ["Button"]
    - path: "src/components/ui/input.tsx"
      provides: "Componente Input estilizado"
      exports: ["Input"]
    - path: "src/components/ui/card.tsx"
      provides: "Componente Card base"
      exports: ["Card"]
    - path: "src/components/ui/badge.tsx"
      provides: "Componente Badge com variantes de plano"
      exports: ["Badge"]
    - path: "src/components/ui/spinner.tsx"
      provides: "Spinner 3 dots bounce"
      exports: ["Spinner"]
  key_links:
    - from: "tailwind.config.ts"
      to: "src/app/globals.css"
      via: "CSS variables referenciadas por rgb(var(--token))"
      pattern: "rgb\\(var\\(--charcoal\\)"
    - from: "src/app/layout.tsx"
      to: "next/font/google"
      via: "Import de Cormorant_Garamond, DM_Sans, JetBrains_Mono"
      pattern: "Cormorant_Garamond"
---

<objective>
Implementar o design system completo da Anja: CSS variables RGB, cores no Tailwind, grain texture SVG, fontes via next/font/google, e componentes base (Button, Input, Card, Badge, Spinner).

Purpose: Define a identidade visual premium do produto. Todos os planos subsequentes dependem destes tokens e componentes.
Output: Design system funcional com tokens de cor, tipografia e 5 componentes reutilizáveis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-fundacao/01-UI-SPEC.md
@.planning/phases/01-fundacao/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS Variables, Tailwind config, fontes e grain texture</name>
  <files>src/app/globals.css, tailwind.config.ts, src/app/layout.tsx</files>
  <read_first>
    .planning/phases/01-fundacao/01-UI-SPEC.md (seções: Tokens de Cor, Typography, Efeitos Globais, Responsividade)
    src/app/globals.css
    tailwind.config.ts
    src/app/layout.tsx
  </read_first>
  <action>
**1. Reescrever `src/app/globals.css`** com os tokens de cor e efeitos:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --charcoal:    26 24 20;
    --char2:       42 38 32;
    --char3:       58 53 47;
    --gold:        201 168 76;
    --gold-light:  232 201 106;
    --cream:       245 240 232;
    --text-muted:  138 130 120;
    --off-white:   253 250 245;
    --danger:      239 68 68;
    --success:     34 197 94;
  }
}

/* Grain texture — SVG feTurbulence inline (RNF-04) */
.grain::before {
  content: '';
  position: fixed;
  inset: -200%;
  width: 400%;
  height: 400%;
  opacity: 0.035;
  pointer-events: none;
  z-index: 9999;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
  background-repeat: repeat;
  background-size: 200px 200px;
}

/* Safe area bottom para iOS home indicator (RNF-02) */
.safe-area-pb {
  padding-bottom: max(8px, env(safe-area-inset-bottom));
}
```

**2. Atualizar `tailwind.config.ts`** com cores customizadas e fontFamily:

```ts
import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        charcoal:     'rgb(var(--charcoal) / <alpha-value>)',
        'char-2':     'rgb(var(--char2) / <alpha-value>)',
        'char-3':     'rgb(var(--char3) / <alpha-value>)',
        gold:         'rgb(var(--gold) / <alpha-value>)',
        'gold-light': 'rgb(var(--gold-light) / <alpha-value>)',
        cream:        'rgb(var(--cream) / <alpha-value>)',
        muted:        'rgb(var(--text-muted) / <alpha-value>)',
        'off-white':  'rgb(var(--off-white) / <alpha-value>)',
        danger:       'rgb(var(--danger) / <alpha-value>)',
        success:      'rgb(var(--success) / <alpha-value>)',
      },
      fontFamily: {
        display: ['var(--font-cormorant)', 'serif'],
        body: ['var(--font-dm-sans)', 'sans-serif'],
        mono: ['var(--font-jetbrains)', 'monospace'],
      },
    },
  },
  plugins: [],
};

export default config;
```

**3. Atualizar `src/app/layout.tsx`** com as 3 fontes via `next/font/google` e grain no body:

```tsx
import type { Metadata } from 'next';
import { Cormorant_Garamond, DM_Sans, JetBrains_Mono } from 'next/font/google';
import './globals.css';

const cormorant = Cormorant_Garamond({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700'],
  style: ['normal', 'italic'],
  variable: '--font-cormorant',
  display: 'swap',
});

const dmSans = DM_Sans({
  subsets: ['latin'],
  weight: ['400', '600'],
  variable: '--font-dm-sans',
  display: 'swap',
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ['latin'],
  weight: ['400'],
  variable: '--font-jetbrains',
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'Anja — Secretaria Executiva com IA',
  description: 'Sua secretaria executiva com IA para empreendedores brasileiros.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="pt-BR"
      className={`${cormorant.variable} ${dmSans.variable} ${jetbrainsMono.variable}`}
    >
      <body className="font-body bg-charcoal text-cream antialiased grain">
        {children}
      </body>
    </html>
  );
}
```

IMPORTANTE:
- NÃO aplicar `transform` ou `will-change` no body (quebra grain position:fixed)
- NÃO usar `font-medium` (500) em nenhum lugar — apenas `font-normal` (400) e `font-semibold` (600)
- Cormorant carrega pesos 300-700 para fases futuras, mas apenas 400 e 600 são usados agora
- DM Sans carrega apenas 400 e 600 (os únicos pesos permitidos)
  </action>
  <verify>
    <automated>cd c:/Users/homer/ccos-ratos/projetos/anja && grep -c "\-\-charcoal" src/app/globals.css && grep -c "rgb(var(--charcoal)" tailwind.config.ts && grep -c "Cormorant_Garamond" src/app/layout.tsx && grep -c "grain" src/app/layout.tsx && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `globals.css` contém `--charcoal:    26 24 20` (grep -c "26 24 20" src/app/globals.css retorna 1)
    - `globals.css` contém `.grain::before` (grep -c "grain::before" src/app/globals.css retorna 1)
    - `globals.css` contém `.safe-area-pb` (grep -c "safe-area-pb" src/app/globals.css retorna 1)
    - `tailwind.config.ts` contém `charcoal: 'rgb(var(--charcoal)` (grep presente)
    - `tailwind.config.ts` contém `fontFamily` com `display`, `body`, `mono`
    - `layout.tsx` importa `Cormorant_Garamond`, `DM_Sans`, `JetBrains_Mono`
    - `layout.tsx` aplica classe `grain` no body
    - `layout.tsx` contém `lang="pt-BR"`
    - Nenhum arquivo contém `font-medium` (grep -r "font-medium" src/ retorna 0 resultados)
    - `npx next build` completa sem erros
  </acceptance_criteria>
  <done>Tokens de cor RGB, grain texture, safe-area utility, Tailwind config com cores + fontes, e root layout com 3 fontes next/font carregando. Build passa.</done>
</task>

<task type="auto">
  <name>Task 2: Componentes base (Button, Input, Card, Badge, Spinner)</name>
  <files>src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/card.tsx, src/components/ui/badge.tsx, src/components/ui/spinner.tsx</files>
  <read_first>
    .planning/phases/01-fundacao/01-UI-SPEC.md (seção: Componentes Base — Button, Input, Card, Badge, Spinner)
  </read_first>
  <action>
Criar diretório `src/components/ui/` e implementar os 5 componentes base conforme UI-SPEC.

**Button (`src/components/ui/button.tsx`):**
- Props: `variant` ('primary' | 'secondary' | 'ghost', default 'primary'), `loading` (boolean), `children`, e todos os HTMLButtonElement props via `ButtonHTMLAttributes<HTMLButtonElement>`
- 3 variantes com estilos EXATOS da UI-SPEC:
  - primary: `bg-gold/10 border border-gold/20 text-gold hover:bg-gold/20`
  - secondary: `bg-char-3 border border-char-3 text-cream hover:bg-char-3/80`
  - ghost: `text-muted hover:text-cream hover:bg-char-3` (sem border)
- Dimensões: `px-4 py-2.5 text-sm font-normal rounded-lg transition-colors duration-150`
- Disabled: `opacity-40 cursor-not-allowed`
- Loading: mostra Spinner + children com `opacity-60`, botao fica disabled
- NÃO usar `font-medium` — usar `font-normal`

**Input (`src/components/ui/input.tsx`):**
- Props: `error` (boolean), e todos os HTMLInputElement props via `InputHTMLAttributes<HTMLInputElement>` + `forwardRef`
- Estilos: `w-full bg-charcoal border border-char-3 focus:border-gold/50 rounded-lg px-3 py-2.5 text-sm text-cream placeholder:text-muted outline-none focus:ring-2 focus:ring-gold/20`
- Estado de erro: `border-danger/60 focus:border-danger focus:ring-danger/20`

**Card (`src/components/ui/card.tsx`):**
- Props: `className` (opcional), `children`, e HTMLDivElement props
- Estilos: `bg-char-2 border border-char-3 rounded-lg p-4`
- className recebido via props deve ser merged (usar template literal, não cn())

**Badge (`src/components/ui/badge.tsx`):**
- Props: `variant` ('free' | 'essencial' | 'executivo' | 'agencias' | 'default'), `children`
- Variantes:
  - free: `text-muted bg-char-3 border border-char-3`
  - essencial: `text-gold bg-gold/10 border border-gold/20`
  - executivo: `text-gold bg-gold/10 border border-gold/20`
  - agencias: `text-gold-light bg-gold/15 border border-gold/30`
  - default: `text-muted bg-char-3 border border-char-3`
- Dimensões: `text-[10px] font-normal px-2 py-0.5 rounded inline-flex items-center`

**Spinner (`src/components/ui/spinner.tsx`):**
- Implementação EXATA da UI-SPEC: 3 dots bounce
- 3 spans `w-1 h-1 rounded-full bg-gold animate-bounce` com `animationDelay` de 0, 0.15s, 0.3s

Todos os componentes:
- Export nomeado (não default)
- 'use client' no topo de cada arquivo (são componentes interativos)
- TypeScript estrito — sem `any`
- NÃO usar bibliotecas externas como `clsx` ou `cn` — usar template literals para class merging
  </action>
  <verify>
    <automated>cd c:/Users/homer/ccos-ratos/projetos/anja && for f in button input card badge spinner; do test -f "src/components/ui/$f.tsx" && echo "$f OK" || echo "$f MISSING"; done && grep -r "font-medium" src/components/ui/ | wc -l && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Arquivos existem: button.tsx, input.tsx, card.tsx, badge.tsx, spinner.tsx em src/components/ui/
    - Cada arquivo tem `'use client'` na primeira linha (grep -l "use client" src/components/ui/*.tsx retorna 5 arquivos)
    - `button.tsx` exporta `Button` (grep -c "export.*Button" src/components/ui/button.tsx retorna 1)
    - `button.tsx` contém 3 variantes: primary, secondary, ghost (grep -c "primary\|secondary\|ghost" src/components/ui/button.tsx >= 3)
    - `input.tsx` usa forwardRef (grep -c "forwardRef" src/components/ui/input.tsx retorna 1)
    - `badge.tsx` contém variantes de plano (grep -c "free\|essencial\|executivo\|agencias" src/components/ui/badge.tsx >= 4)
    - `spinner.tsx` contém `animate-bounce` (grep -c "animate-bounce" src/components/ui/spinner.tsx retorna 1)
    - ZERO ocorrencias de `font-medium` (grep -r "font-medium" src/components/ui/ retorna vazio)
    - `npx next build` completa sem erros
  </acceptance_criteria>
  <done>5 componentes base criados com variantes, loading states, e acessibilidade. Nenhum font-medium. Build passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhum neste plano | Design system é puramente visual, sem input de usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | T (Tampering) | SVG inline no grain | accept | SVG é hardcoded no CSS, não aceita input externo. Risco zero. |
</threat_model>

<verification>
```bash
cd c:/Users/homer/ccos-ratos/projetos/anja
# Verificar tokens de cor
grep -c "26 24 20" src/app/globals.css
# Verificar grain
grep -c "grain::before" src/app/globals.css
# Verificar fontes no layout
grep -c "Cormorant_Garamond" src/app/layout.tsx
# Verificar componentes existem
ls src/components/ui/*.tsx | wc -l  # deve ser 5
# Verificar zero font-medium
grep -r "font-medium" src/ | wc -l  # deve ser 0
# Build
npx next build
```
</verification>

<success_criteria>
- CSS variables RGB definidas e Tailwind configurado com cores customizadas
- Grain texture aplicada via .grain::before no body
- 3 fontes carregando via next/font/google (Cormorant, DM Sans, JetBrains Mono)
- 5 componentes base exportando corretamente
- Zero ocorrencias de font-medium no código
- Build passa sem erros
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao/01-02-SUMMARY.md`
</output>
